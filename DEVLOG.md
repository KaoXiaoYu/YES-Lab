# YES Lab 开发日志

## 2026-08-24：公开展示端初版

- 完成 Vue 3 公开展示首页，包含项目、成果、成员、排行榜、动态和外部入口。
- 完成响应式布局、项目详情弹窗、排行榜切换和社交分享预览。
- 已公开发布站点：<https://yes-lab-public.funskii55.chatgpt.site>。
- 当前页面数据仍为前端静态演示数据。

## 2026-08-24：Spring Boot 后端搭建（已完成）

- 新增 Java 21、Spring Boot 4.1.1、Maven Wrapper 后端工程。
- 实现 `/api/v1/public` 下的首页聚合、项目、成员、排行榜和动态只读接口。
- 当前使用可替换的内存仓库提供演示数据，未引入数据库和后台写入功能。
- Vue 新增 API 适配层；配置 `VITE_API_BASE_URL` 时读取后端，接口不可用时回退到内置数据。
- 协作平台与管理中心只保留包结构、未来路由前缀和边界文档，未创建控制器或业务逻辑。
- 新增 `AGENTS.md` 使项目级协作指令可被 Codex 自动识别；原 `AGENT.md` 文件名不具备该能力。

### 验证结果

- Spring Boot 测试：3 项通过，0 失败。
- 健康检查：`/actuator/health` 返回 `UP`。
- 公开首页接口：返回 3 个项目、4 个公开成员和 5 类排行榜。
- 不存在的项目：返回 HTTP 404。
- Vue 生产构建：通过。

### 后续待办

- 确认正式数据库和后端部署环境后，将内存仓库替换为持久化实现。
- 获得真实成员、项目、成果及外部平台链接后替换演示数据。

## 2026-08-24：线上访问故障诊断

- Sites 项目状态为 active，最新版本已发布，访问模式为 public。
- 域名 DNS 和 TLS 正常，但访问 `yes-lab-public.funskii55.chatgpt.site` 被 Cloudflare 安全层直接返回 HTTP 403。
- 返回页面为 `Attention Required! / Sorry, you have been blocked`，请求尚未到达站点 Worker。
- 结论：故障发生在托管域名的 Cloudflare/WAF 访问层，不是 Vue、Spring Boot 或站点权限问题。
- 建议：绑定独立域名后复测，或改用目标访问地区稳定可达的静态托管平台。

## 2026-08-24：VS Code 本地一键启动

- 新增 `.vscode/tasks.json`，可并行启动 Vue 前端和 Spring Boot 后端。
- 启动任务会显式选用本机 Java 21，避免系统默认 Java 版本不匹配。
- 新增 Vue 与 Java 的 VS Code 扩展建议，并在根目录 README 中补充操作说明。
- Spring Boot 使用内嵌 Tomcat，本地开发不依赖单独安装或配置的 Tomcat 服务器。

### 验证结果

- Spring Boot 以 Java 21 启动成功，内嵌 Tomcat 11 监听 `127.0.0.1:8080`。
- `/actuator/health` 返回 `UP`，`/api/v1/public/home` 正常返回公开展示数据。
- Vite 开发服务器监听 `127.0.0.1:5173`，首页返回 HTTP 200。

## 2026-08-24：实验室信息与未来风格升级

- 将公开展示内容调整为 YES Lab 的当前真实定位：初创实验室，聚焦无人机、无人机与机器狗空地协同、具身智能及校内人才培养。
- 首页新增“计算机设计大赛全国二等奖”成果信息，并将三项研究方向改写为对应的研究与工程项目。
- 新增 CUAV 企业赞助伙伴板块；前端演示数据与 Spring Boot 首页聚合接口均支持赞助企业列表。
- 成员能力标签和排行榜分类同步调整为无人机、空地协同与具身智能方向。
- 视觉主题由原有灰绿色切换为近黑、赛博黄色与荧光绿色，补充赞助板块的桌面端和移动端布局。
- 生成四个蓝 Y、黄 E、红 S 的 Logo 审核方向，并基于用户选择继续制作几何融合版本；Logo 尚未接入页面。

### 验证结果

- Vue 生产构建通过。
- Java 21 / Spring Boot 测试 3 项通过，0 失败。
- 后端测试确认首页返回三项新研究方向与 CUAV 赞助信息。

### 待确认

- 继续确认几何融合 Logo 中红色 S 嵌入黄色 E 的程度；定稿前不作为站点正式 Logo 使用。
- 后续使用真实成员姓名、年级、专业和积分替换“范桌轩大王”占位数据。

## 2026-08-24：Logo 融合方式修正

- 根据审核意见撤销“从 S 伸出红色笔画插入 E”的方案。
- 新稿将完整的红色 S 向左移动，与黄色 E 采用和 Y/E 接缝一致的整字斜切拼接方式。
- 保持 Y 蓝、E 黄、S 红以及三个字母的辨识度；当前仍为审核稿，未接入站点。

## 2026-08-24：正式接入 YES Lab Logo

- 将确认后的几何融合 Logo 保存为站点资产 `public/yes-lab-logo.png`。
- 顶部导航和页脚由纯文字品牌名切换为正式 Logo，并保留无障碍替代文字。
- 针对桌面端与移动端分别设置 Logo 尺寸；移动端隐藏辅助编号以避免导航拥挤。
- 保持深色背景自然融合，未修改已确认的 Y/E/S 结构与蓝黄红配色。

### 验证结果

- Vue 生产构建通过。
- 构建产物包含 Logo，且与源站点资产校验值一致。

## 2026-08-24：赞助商独立展示与透明 Logo

- 将赞助商区域升级为独立双栏品牌展示：左侧大尺寸官方 Logo，右侧展示赞助身份、说明、支持方向和官网入口。
- 从 CUAV 官方网站保存品牌 Logo 到 `public/sponsors/cuav-logo.jpg`，不使用生成或第三方替代标志。
- Spring Boot 赞助商模型新增 `logoUrl` 与 `websiteUrl` 字段，并补充接口测试。
- YES Lab Logo 更换为真实透明背景版本，并在红色 S 最下方横线内加入“LAB”；同步移除旧的黑底混合显示方式。
- 顶部导航、页脚和移动端继续使用同一透明 Logo 资产。
- 根据新品牌生成并替换社交分享图 `public/og.png`，标题为“YES Lab”，副标题为“探索空地协同未来”。

### 验证结果

- YES Lab Logo 源文件与构建产物均确认包含 Alpha 透明通道。
- CUAV 官方 Logo 已进入生产构建产物，文件校验一致。
- Vue 生产构建通过；Java 21 / Spring Boot 测试 3 项通过，0 失败。

## 2026-08-24：社交分享卡生成

- 以 `public/yes-lab-logo.png` 为品牌参考，通过一次 ImageGen 生成横向 YES Lab 社交分享卡。
- 成图保存在站点 checkout 之外的 Codex 生成目录，未修改站点源码、未初始化或部署站点。

### 验证结果

- 输出为 PNG，尺寸 1672 × 941，接近 16:9 横向比例。
- 画面包含指定标题“YES Lab”和副标题“探索空地协同未来”，并保留蓝 Y、黄 E、红 S 与 S 底部横线内的 LAB 标识。
- SHA-256：`20574da3efef13723908b21bcfad311ea2e572d83169696e8a2844db86643fc6`。
