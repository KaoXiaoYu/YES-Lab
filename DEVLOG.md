# YES Lab 开发日志

> 本日志已于 2026-08-25 合并重复的 UI、Logo、启动与验证记录，仅保留关键决策、当前能力和后续待办。

## 当前状态

- 前端：Vue 3 + Vite，包含公开展示、公开成员主页、登录注册、游客报名、成员个人主页、成员/招新管理、项目团队空间和竞赛成果管理。
- 后端：Java 21 + Spring Boot 4.1.1、Spring Security、JWT、JPA；本地使用 H2，生产配置使用 MySQL 8.4 + Flyway。
- 角色：教师与核心学生均为系统管理员；普通成员维护个人主页；游客只维护本人报名并查看进度。
- 尚未实现：测验、写题、积分计算、比赛记录删除/导入和项目即时聊天；现阶段仅保留相应权限或字段边界。

## 2026-08-24：项目基础与公开展示端

- 完成 Vue 公开首页和 Spring Boot `/api/v1/public` 只读接口，覆盖实验室简介、研究方向、项目、成果、成员、排行榜、动态和赞助伙伴。
- 明确品牌全称为 `Yichun Embodied Science`；研究方向为无人机、无人机与机器狗空地协同、具身智能及相关人才培养。
- 加入计算机设计大赛全国二等奖、江西省智能机器人大赛飞行巡航定点赛道省赛二等奖、全国智能汽车大赛平衡轮腿组华东赛赛区三等奖。
- 建立 CUAV 独立赞助商展区，使用其官方 Logo 和官网链接。
- 定稿融合式 YES Lab Logo：Y/E/S 为蓝/黄/红，LAB 在 S 底部横线内透明镂空；同时保留纯白版与其他审核版本。
- 使用 UI/UX Pro Max 确立 Swiss Modernism 2.0 设计系统，完成响应式网格、键盘操作、可见焦点、触控尺寸和减少动态效果适配。
- 成员区调整为指导老师、核心成员和实时榜单；指导老师暂用“汤洪大王”，其他缺失内容继续使用“范桌轩大王”占位。

### 本地开发与部署记录

- 新增 VS Code 一键启动任务，显式使用本机 Java 21；Spring Boot 使用内嵌 Tomcat，无需配置独立 Tomcat。
- 本地前端默认 `127.0.0.1:5173`，后端默认 `127.0.0.1:8080`；Vite 端口被占用时以前端终端输出为准。
- 曾发布公开展示版本到 `yes-lab-public.funskii55.chatgpt.site`；该域名出现过 Cloudflare/WAF 403，诊断结果与 Vue/Spring Boot 代码无关。
- 线上版本目前只适合公开展示；账号和管理功能依赖独立 Spring Boot 服务，未在 Sites 中部署后端。

## 2026-08-25：账号、权限与招新流程

- 建立 `TEACHER`、`CORE_STUDENT`、`MEMBER`、`VISITOR` 四类角色和模块化权限；教师与核心学生当前权限完全一致。
- 使用 Spring Security、BCrypt 和 HS256 JWT 实现注册、登录、当前会话和无状态接口鉴权；新注册账号固定为游客。
- 实现“报名 → 初筛 → 面试 → 技能测试 → 试用期 → 正式成员”的受控流程，以及拒绝分支、面试官分配、评分、评价、建议标签和未来测验关联字段。
- 每次状态变更保存原状态、目标状态、操作人、时间和备注；试用期游客可一键转换为正式成员，原报名和面试记录继续保留。
- 实现成员固定字段、能力标签和成长数据边界；成员本人只能编辑头像、内部联系方式、主页标语和富文本介绍。
- 个人主页接入 Tiptap；后端使用 OWASP HTML Sanitizer 白名单清洗 HTML。
- 本地演示账号：`teacher`、`core`、`member`；密码和生产环境安全配置见 README。

### 验证

- Java 21 下 7 项测试通过；覆盖角色权限、完整招新流转、一键转成员、JWT 和富文本安全清洗。
- Vue 生产构建通过，登录、报名、个人主页与招新管理路由可用。

## 2026-08-25：成员管理与公开成员主页

- 新增系统管理员成员管理页，可搜索和筛选成员，并维护姓名、成员编号、角色、状态、专业、班级、年级、内部联系方式和能力标签。
- 新增公开成员目录与详情 API；公开响应不包含成员编号和内部联系方式，只展示正式成员。
- 教师资料不展示专业、班级、年级、积分和排名；切换为教师角色时后端会清空不适用的学籍字段。
- 个人主页改为只读展示，富文本编辑器迁移到独立编辑页，通过“编辑个人主页”按钮进入。
- 登录后，公开首页右上角由“登录/注册”切换为当前账号的头像和姓名，并跳转到相应成员页面或游客报名页。
- 公开首页的指导老师、核心成员和榜单项均可进入对应成员公开主页；旧静态占位成员仍兼容有限详情。
- 更新成员系统顶部导航，系统管理员可直接进入“成员管理”和“招新管理”。
- 根据 UI/UX Pro Max 建议，成员管理采用桌面双栏目录与移动端纵向卡片布局，避免数据表在小屏横向溢出。

### 验证

- Java 21 下 8 项测试通过，0 失败；新增覆盖普通成员禁止访问管理接口、管理员更新成员、教师公开字段精简和内部字段不外泄。
- Vue 生产构建通过，新增页面按路由拆分；富文本编辑器只在编辑页加载。
- `git diff --check` 通过。

## 2026-08-25：项目团队管理

- 新增项目实体、类型/状态、阶段目标、进度、成果、仓库/文档链接和公开展示开关；项目可选关联一名教师作为指导老师。
- 系统管理员创建项目并指定负责人；负责人管理团队名称、成员和项目管理员，项目管理员可编辑项目资料，普通成员只读自己参与的项目。
- 新增项目列表、创建页和团队空间；公开项目接入首页项目区，公开响应不暴露项目管理员和内部权限字段。
- “项目群”当前实现为团队与角色协作空间，不包含即时消息、群文件或已读状态。

### 验证

- Java 21 下 10 项测试通过，0 失败；覆盖系统管理员创建、教师指导关联、普通成员负责人上下文权限、错误指导老师校验和非公开项目隔离。
- Vue 生产构建通过；项目页面按路由拆分，公开首页与成员系统项目卡片样式已隔离。
- `git diff --check` 通过。

## 2026-08-25：竞赛成果、新闻与公开比赛详情

- 新增比赛实体、队员关联、可选指导老师与项目关联、生命周期、省赛/国赛时间、证书、比赛图集、审核状态、首页开关和手动排序。
- 任意实验室成员可作为队长提交；已结束比赛强制上传证书并进入管理员审核，未结束比赛强制填写省赛/国赛时间和指导老师。
- 系统管理员可审核、驳回、修改记录和控制首页展示；更换证书会使已结束比赛重新进入待审核，证书仅队长和管理员可读。
- 新增外部新闻引用管理，只保存标题、来源、原文链接、摘要和发布日期；公开新闻按日期倒序。
- 公开首页成果区改为“新闻 + 比赛成果”双栏；比赛栏按管理员顺序，点击后进入独立图文详情页。
- 公开比赛详情页展示比赛文字、图集、获奖信息、队长、指导老师、关联项目和参赛成员；已关联成员可进入对应公开主页，获奖结果也会自动进入其主页成果记录。
- 本地证书与图片保存至 `backend/data/achievements/`，限制格式、大小并校验文件签名；生产环境待迁移至带私有访问控制的对象存储。

### 验证

- 新增 3 项成果模块集成测试，覆盖普通成员越权审核拦截、证书/图片上传、管理员认证与首页排序、公开图文详情、成员成果关联、未结束比赛约束和新闻日期排序；3 项均通过。
- Java 21 全量回归共 13 项测试通过，0 失败、0 错误、0 跳过；本地 Spring Boot 健康检查为 `UP`。
- Vue 生产构建通过，竞赛列表、队长表单、成果管理和公开详情均按路由拆分。
- `git diff --check` 通过。

## 2026-08-25：项目主图

- 为项目增加主图元数据和独立文件存储；负责人、项目管理员和系统管理员可上传或替换，普通成员无编辑权限。
- 内部项目主图通过 JWT 鉴权读取，公开项目通过公开只读接口读取；格式限制为 JPG、PNG、WebP，单文件最大 8MB，并校验文件签名。
- 项目列表、项目团队空间、公开首页卡片和公开项目弹窗均展示主图；未上传时使用透明底 YES Lab Logo 作为统一默认图。
- 项目资料编辑弹窗增加 16:10 预览、文件选择、客户端格式/大小提示和独立上传状态；本地文件默认保存在 `backend/data/projects/covers/`。

### 验证

- Java 21 全量回归共 13 项测试通过，0 失败、0 错误、0 跳过；新增覆盖负责人上传、JWT 内部读取与公开项目主图读取。
- Vue 生产构建通过；Spring Boot 重启后健康检查为 `UP`，Vite 首页可访问。
- 当前环境无可用浏览器实例，未完成自动截图视觉检查；已通过构建、响应式样式审查和运行服务检查。

## 后续待办

- 正式服务器首次上线前完成域名解析、HTTPS 签发、MySQL/Flyway 空库与已有库演练、备份恢复演练；生产环境关闭演示账号初始化并配置独立 JWT 密钥和正式 CORS 域名。
- 后续增加密码重置、登录限流与告警、JWT 双密钥平滑轮换、全局操作审计、独立的内部联系方式查看权限。
- 后续实现头像文件上传，以及测验和积分模块；项目即时聊天如需加入，应先补充消息、文件、已读与内容治理规则。
- 使用真实成员、项目、仓库和社交平台资料替换占位内容。

## 2026-08-25：公开主页内容管理

- 新增系统管理员专用“主页编辑”页面，按品牌与首屏、栏目文案、展示选择、概览与奖项、首页动态、赞助伙伴和外部入口分区维护。
- 新增持久化主页配置和 `/api/v1/admin/homepage` 读写接口，使用 `CONTENT_MANAGE` 权限保护；教师与核心学生可管理，普通成员和游客不可访问。
- 公开首页已读取保存后的实验室简介、研究方向、首屏与栏目文案、概览条、奖项、备用动态、赞助商、外部入口，以及指导老师、核心成员和项目的展示选择与顺序。
- 成员、项目、比赛和新闻仍以各自业务模块为唯一数据来源；主页编辑页提供管理入口与首页展示选择，不复制详细记录。
- 根据 UI/UX Pro Max 规范采用分区编辑、明确字段标签、保存中禁用与状态反馈、粘性保存栏、键盘焦点和移动端单列适配。

### 验证

- Java 21 全量回归共 14 项测试通过，0 失败、0 错误、0 跳过；新增覆盖普通成员越权拦截、教师保存、核心学生读取和公开首页同步。
- Vue 生产构建通过，主页编辑页保持路由拆分；`git diff --check` 通过。

## 使用说明补充

- 项目在公开首页的主图和资料从“项目团队 → 项目空间 → 编辑项目资料”维护；需同时开启“允许公开展示”。
- 比赛首页展示顺序和新闻引用由系统管理员在“成果管理”页面维护；实验室简介、首屏、栏目文案、奖项、赞助伙伴、外部入口及首页成员/项目展示选择由“主页编辑”页面维护。

## 2026-08-26：首页导航与第二栏扩展

- 首屏研究方向改为“名称 + 跳转地址”可维护数据，支持页内锚点、站内绝对路径和 HTTP(S) 外链；旧主页数据读取时自动补齐为 `#projects`。
- 后端对研究方向 URL 做白名单校验，拒绝 `javascript:`、协议相对外链和无主机的 HTTP(S) 地址；同时保留旧字段兼容已保存配置。
- “关于我们”第二栏从重复的奖项列表改为可编辑特色卡片，可维护区域眉题、标题、卡片标题/描述和排序；比赛继续以成果管理为正式数据源，主页奖项配置仅作无正式数据时的备用。
- 页内锚点改为绑定实际栏目标题，统一使用响应式顶栏高度作为偏移，移除额外 16px 空隙，并兼容移动端和横屏顶栏。
- 按 UI/UX Pro Max 完成显式表单标签、禁用态、链接焦点、响应式卡片和稳定悬停状态复核。
- README 新增 Git 部署与服务器数据说明：代码与业务数据分离，`backend/data/` 不进 Git；正式上线建议 PostgreSQL + Flyway、独立持久化上传目录和定期备份。

### 验证

- Java 21 全量回归共 14 项测试通过，0 失败、0 错误、0 跳过；新增研究方向外链保存和非安全协议拦截断言。
- Vue 生产构建通过；`git diff --check` 通过。
- Spring Boot 已使用 Java 21 重启，健康检查为 `UP`；公开首页 API 返回 3 个可跳转研究方向，Vite 首页返回 HTTP 200。
- 未进行自动截图视觉验收；已完成构建、响应式样式审查与运行态接口检查。

## 2026-08-26：MySQL、正式 JWT 会话与生产部署基线

- 生产数据库确定为 MySQL 8.4，加入 Connector/J、Flyway 和独立 `prod` profile；生产环境使用 `ddl-auto=validate`，演示数据初始化固定关闭，数据库连接、JWT 密钥和 CORS 来源均由环境变量注入。
- 建立 V1 完整表结构与 V2 刷新会话迁移。空库执行完整建表；已有未纳管 MySQL 库登记为 V1 后只执行 V2。富文本、主页 JSON、比赛/项目描述等列统一扩为 `LONGTEXT`，避免 MySQL `TINYTEXT` 截断。
- JWT 访问令牌缩短为 15 分钟并增加 `jti`；新增 48 字节随机刷新令牌，服务端只存 SHA-256 摘要，支持轮换、吊销和退出登录。
- 登录页新增明确的“记住我的登录状态”复选框：不勾选时为最长 12 小时的浏览器会话 Cookie，勾选时为 30 天持久 Cookie；生产 Cookie 使用 `HttpOnly + Secure + SameSite=Lax`。访问 JWT 改为仅保存在前端内存，旧 `sessionStorage` 令牌会被清除。
- 前后端改为同源 `/api`：本地由 Vite 代理，生产由 Caddy 代理，减少跨域 Cookie 差异；登录表单按 UI/UX Pro Max 保留原生语义、可见说明、键盘焦点和密码管理器兼容。
- 新增 Vue/Caddy 与 Java 21 多阶段镜像、MySQL/API/Web Compose 编排、低内存 JVM/MySQL 参数、健康检查、仓库外数据目录及 Caddy 自动 HTTPS。
- 新增 GitHub Actions：`main` 推送先完成 Vue 构建与 Java 测试，再向 `ghcr.io/kaoxiaoyu` 发布 `latest` 和提交 SHA 镜像；服务器无需承担编译。
- 新增升级前 MySQL + 上传文件备份脚本、安全快进部署脚本、生产环境示例和 Debian 12 / Ubuntu LTS 通用部署手册；明确禁止 `down -v`，并要求首次上线和后续定期进行恢复演练。

### 验证

- Java 21 全量回归共 15 项测试通过，0 失败、0 错误、0 跳过；新增覆盖未勾选会话 Cookie、勾选后精确 30 天、刷新令牌轮换、旧令牌失效和退出吊销。
- Vue 生产构建通过；`git diff --check` 与两个部署脚本 `bash -n` 通过；Compose YAML 可被本地 YAML 解析器读取。
- 本地 Spring Boot 已用 Java 21 重启，真实 HTTP 登录返回 `Max-Age=2592000` 的 HttpOnly Cookie，刷新接口返回 200 且 Cookie 完成轮换；Vite 同源代理健康检查返回 200。
- 已使用 Hibernate MySQL 方言生成元数据并核对 V1/V2 字段类型；当前电脑没有运行中的 Docker daemon/Compose 插件，因此尚未执行真实 MySQL 容器迁移、镜像构建和 Caddy HTTPS 实机测试，这三项列为服务器首次发布前的强制验收。

## 2026-08-26：2 核 2 GB Ubuntu 24.04 服务器容量评估

- 确认 Ubuntu 24.04 LTS（Noble）属于 Docker Engine 官方支持版本，可继续使用现有 Docker Compose、MySQL 8.4、Spring Boot API 与 Caddy 部署方案。
- 当前 MySQL、API、Web 三个容器内存上限合计 800 MB；2 GB 内存可满足初期低并发运行，正式上线时建议配置 2 GB swap，仅用于吸收启动、备份和偶发请求峰值。
- 2 核 CPU 与 20 Mbps 带宽足以承载实验室公开展示、成员管理和少量并发上传；不建议在本机托管视频或进行图片转码、批量计算等重任务。
- 40 GB SSD 是主要容量约束。现有备份脚本每天同时备份数据库和全部上传文件并默认保留 14 天，图片增长后会重复占用大量空间；正式上线前应改为较短的本机留存配合异地备份，并限制 Docker 日志大小。
- 200 GB 月流量折合整月平均约 0.62 Mbps；普通图文访问够用，但需压缩首页和比赛图片并使用浏览器缓存，视频应使用外部视频平台或对象存储/CDN。

### 验证

- 核对 Compose 内存配置：MySQL 320 MB、API 384 MB、Web 96 MB，合计硬限制 800 MB。
- 核对备份脚本：上传目录采用完整 `tar.gz` 快照，默认本机保留 14 天。
- 核对 Docker 官方 Ubuntu 支持列表：Ubuntu Noble 24.04 LTS 受支持。本次仅进行容量与部署兼容性评估，未修改运行代码。

## 2026-08-26：Ubuntu 24.04 SSH 一键部署

- 新增 `deploy/scripts/bootstrap-ubuntu.sh`，支持在全新 Ubuntu 24.04 服务器通过 SSH 一次完成 Docker 官方 APT 源安装、2 GB swap、生产密钥、仓库外数据目录、镜像拉取、健康检查、首个管理员和自动备份配置。
- 脚本为幂等设计：已有 `deploy/.env.production`、MySQL 数据、上传文件和账号密码均不会被覆盖；Docker 包冲突时停止并要求人工处理，不擅自卸载已有运行环境。
- 新增独立的生产首管理员初始化器，只创建一个 `TEACHER` 管理员与规范成员档案，不启用三个演示账号或演示项目；已有同名账号不会被修改或提权。
- 首个管理员使用运行时随机密码初始化；写入数据库并校验角色后，脚本立即以关闭初始化的环境重建 API，使明文初始密码不写入 `.env.production`，只在 SSH 终端显示。
- 为三个容器统一增加 `10 MB × 3` 的 JSON 日志轮转，避免日志占满 40 GB 磁盘；本机完整备份默认留存从 14 天调整为 7 天，并为备份清理增加绝对路径保护。
- 新增每天约 03:30 执行的 `yeslab-backup.timer`，生产手册改为 Ubuntu 24.04 SSH 操作流程，并补充 GHCR 默认私有、域名、安全组、健康检查和日常更新说明。

### 验证

- Java 21 全量回归共 18 项测试通过，0 失败、0 错误、0 跳过；新增 3 项覆盖首管理员创建、已有账号不提权和弱初始密码拒绝。
- Vue 生产构建通过；三个部署脚本 `bash -n`、Compose YAML 解析和 `git diff --check` 通过。
- 本机 Docker CLI 没有 Compose 插件且 Docker daemon 未运行，因此未执行真实 Ubuntu 安装、GHCR 拉取、MySQL/Flyway、systemd timer 与 Caddy HTTPS 实机测试；需在服务器首次部署时按手册逐项验收。

### 上线待办

- 将本次代码推送到 GitHub `main` 并等待 Actions 发布新 API/Web 镜像；随后将两个 GHCR 包设为 Public，或准备只读 Packages 令牌。
- 配置真实域名解析及云安全组 TCP 22/80/443、UDP 443，再通过 SSH 运行首次部署脚本并保存终端输出的管理员初始密码。
- 上线后检查容器健康、HTTPS、登录、上传、自动备份，并尽快配置异地备份和完成恢复演练。

## 2026-08-26：生产管理员初始化说明

- 正式环境不需要、也不建议直接执行 SQL 添加管理员；Ubuntu 首次部署脚本会通过 Spring Security 的密码编码器创建教师管理员，并在同一事务中建立对应成员档案和能力标签。
- README 中的 `teacher`、`core`、`member` 固定密码仅供本地演示环境使用，生产 profile 不加载这些演示账号。
- 若生产库已经启动但没有管理员，可重新运行部署引导脚本并用 `--admin-user` 指定一个未占用的登录名；初始化器不会修改或提权已有同名账号，避免意外接管账号。

## 2026-08-26：服务器 GitHub 认证排障

- 确认服务器克隆失败的根因是 GitHub 不再支持使用账号密码进行 HTTPS Git 认证；首次克隆失败导致 `/opt/yes-lab` 未创建，后续 `cd` 和部署脚本报错均为连锁结果。
- 正式部署手册新增私有仓库只读 Deploy Key 流程，包括独立 Ed25519 密钥、GitHub 仓库绑定、官方主机指纹核验、SSH 克隆和仓库级 `core.sshCommand`，确保后续自动发布脚本可持续执行 `git fetch`。
- HTTPS 方式只能使用 GitHub 用户名配合 PAT；生产服务器推荐只读 Deploy Key，且不授予写权限。
- Deploy Key 应复制 `/root/.ssh/yeslab_deploy.pub` 的完整单行内容，格式为 `ssh-ed25519 <Base64 公钥> yes-lab-production`；不得复制无 `.pub` 后缀的私钥，也不要把 GitHub 主机的 `SHA256:` 指纹当作公钥。
- 首次部署已在生成 swap 与生产 `.env.production` 后安全停止于 GHCR `denied`；原因范围为镜像尚未由 Actions 发布或两个 Packages 仍为 Private，MySQL/API/Web 尚未启动。处理包可见性或完成 `docker login ghcr.io` 后可直接重跑引导脚本，已有生产密钥会原样复用。

## 2026-08-26：GitHub Actions Web 镜像构建修复

- 通过 GitHub CLI 读取失败运行 `32945350702` 的完整日志，确认 Web 镜像并非 GHCR 权限或 Caddy 标签失败，而是 Docker 构建阶段缺少 `/workspace/.openai/hosting.json`，导致 `@openai/sites-vite-plugin` 在 `closeBundle` 抛出 `ENOENT`。
- 调整 `.dockerignore`，仅忽略 `.openai` 下的其他内容并明确保留 `hosting.json`；Web Dockerfile 在执行 `npm run build` 前复制该文件，保持 Sites 构建配置完整。
- GitHub Actions 镜像矩阵设置 `fail-fast: false`，Web 或 API 任一镜像失败时，另一个任务仍会继续并给出独立结果，避免无诊断信息的取消状态。

### 验证

- `.openai/hosting.json` 已由 Git 跟踪，Vue 生产构建、工作流 YAML 解析和 `git diff --check` 均通过。
- 当前本机没有可用 Docker daemon，实际容器构建与 GHCR 推送需提交本次修复后由下一次 GitHub Actions 运行验证。

## 2026-08-26：仓库与镜像可见性方案

- 通过 GitHub CLI 确认 `KaoXiaoYu/YES-Lab` 当前仍为 Public；可以改为 Private，现有 Actions 使用仓库 `GITHUB_TOKEN` 的 `contents: read` 与 `packages: write`，不依赖仓库公开状态。
- 推荐源代码仓库和两个 GHCR 镜像均保持 Private：服务器使用只读 Deploy Key 拉取 Git，使用仅含 `read:packages` 的 classic PAT 登录 GHCR；Docker 登录状态可供后续发布脚本复用。
- GHCR Container Package 的可见性与仓库可以独立配置；若镜像改为 Public，服务器可匿名拉取，但 GitHub 官方提示公开后的 Package 不能再改回 Private，因此应在首次成功发布镜像后谨慎选择。

## 2026-08-26：私有 GHCR 镜像拉取排障

- 通过 GitHub CLI 确认 `KaoXiaoYu/YES-Lab` 已设为 Private；最新 Actions 运行 `32951964039` 的测试、API 镜像和 Web 镜像任务全部成功，两个 `latest` 镜像已经发布。
- 服务器返回 `unauthorized` 的根因是 Docker 尚未登录私有 GHCR，或登录使用的令牌缺少 `read:packages` 权限；这不是镜像构建失败。
- Compose 中 API 与 MySQL 显示 `Interrupted` 是 Web 镜像拉取失败后并行任务被取消的连锁结果，并不表示 MySQL 镜像或数据库发生故障。
- 处理方式为使用 GitHub 用户名 `KaoXiaoYu` 和具有 `read:packages` 权限的 classic PAT 执行一次 `docker login ghcr.io`，随后原样重跑部署引导脚本；已有 `.env.production` 和仓库外数据目录不会被覆盖。

### 验证

- GitHub 仓库可见性为 `PRIVATE`；Actions 运行 `32951964039` 总结论为 `success`，三个任务均为 `success`。
- 本次仅完成远程状态核验与部署故障诊断，未连接生产服务器，也未改动生产配置或用户数据。
- 服务器实测 `mysql:8.4` 可完整拉取，而 `yes-lab-web:latest` 与 `yes-lab-api:latest` 均返回 `denied`，进一步排除公网连接、Docker daemon 和 Docker Hub 故障，确认问题仅限私有 GHCR 认证。
- 本地 GitHub CLI 在未授权 `read:packages` 时访问两个包同样返回 HTTP 403，和服务器症状一致；应新建仅含 `read:packages` 的 GitHub classic PAT，不能使用 Fine-grained token、GitHub 密码或仓库 Deploy Key 代替。
- 服务器令牌响应头实测为 `x-oauth-scopes: repo`，确认所用 classic PAT 未授予 `read:packages`；需要新建正确的只读 Packages 令牌，成功后撤销权限过宽的旧令牌。
- 后续拉取从 `root` 切换到 `ubuntu` 用户后出现 Docker socket `permission denied`；部署引导需要 root 权限，GHCR 登录信息也按 Linux 用户隔离，因此应在同一个 root shell 中完成 `docker login`、镜像拉取和部署，避免不同用户凭据不一致。

## 2026-08-26：生产 API 首次启动未通过健康检查

- 私有镜像拉取问题处理后，部署推进到首个管理员初始化阶段，但 `yes-lab-api-1` 被判定为 `unhealthy`，引导脚本在第 206 行安全停止；MySQL 数据目录和上传文件均未删除。
- 第 206 行实际执行的是带临时管理员环境变量的 `docker compose up --wait api`；脚本行号本身不是根因。需通过 API 容器日志、退出状态、`OOMKilled` 标记和 healthcheck 输出区分应用启动异常、Flyway/JPA 数据库错误、健康接口 503 或内存限制问题。
- 在取得服务器只读诊断输出前不修改生产配置、不重建数据库，也不重复创建管理员；部署脚本保持可安全重跑。
- 服务器日志确认 API 可连接 MySQL 8.4，容器未被 OOM kill；实际异常为 `Schema validation: missing table [accounts]`。启动日志没有 Flyway 记录，说明 Hibernate 校验发生前数据库迁移根本未运行。
- 根因是 Spring Boot 4 将 Flyway 自动配置拆分到独立模块，原 POM 只有 `flyway-core` 与 `flyway-mysql`，未引入 `spring-boot-flyway`；已将核心依赖改为官方 `spring-boot-starter-flyway` 并保留 MySQL 专用模块。
- 测试环境继续使用 H2 与 Hibernate `create-drop`，因此在测试配置中显式关闭 MySQL 专用 Flyway 迁移；生产 `application-prod.yml` 仍启用 Flyway，并将在空库依次执行 V1、V2。

### 验证

- Java 21 下后端 18 项测试全部通过，0 失败、0 错误、0 跳过。
- Maven 依赖树确认包含 `spring-boot-starter-flyway:4.1.1`、`spring-boot-flyway:4.1.1`、`flyway-mysql:12.4.0` 与传递依赖 `flyway-core:12.4.0`。
- 当前终端默认 Java 为 8，首次测试因 class version 不兼容未执行；显式使用本机 Java 21 后验证通过，该环境问题与本次应用修复无关。
- 修复提交 `aac1fe4` 已推送到 `origin/main`，GitHub Actions 运行 `32959657354` 的测试、API 镜像与 Web 镜像任务全部成功；新 API `latest` 镜像索引摘要为 `sha256:642bee9f774ec54dcda37cb8b5f38e9cf55eb20b2e39a4a3ddc96088ae6cf888`。
- 服务器重试后 API 仍在约 127 秒后被判定为 `unhealthy`；需核对服务器实际镜像摘要和本次启动日志，并查询 `flyway_schema_history`，确认是旧镜像未替换还是新迁移出现具体的 MySQL 执行/结构校验错误。
- 服务器实际运行摘要为 `sha256:62579f0ffb61138b608da880f821d934f65beca4d61312c47d0ace690e8de959`；通过上一轮 Actions 日志反查，确认该摘要属于修复前提交 `bb79552`，不是 Flyway 修复提交 `aac1fe4` 的新镜像。
- 时间线显示服务器约 18:44 启动旧 API，而新镜像到 18:45:49 才推送完成，因此属于在 Actions 完成前重跑部署造成的发布竞态；数据库仍无 `flyway_schema_history`，也未执行任何业务迁移。Actions 完成后重新拉取 `latest` 并重跑引导即可，无需新增代码修复或手工 SQL。

## 2026-08-26：首页刷新时短暂显示默认内容诊断

- 确认首页配置已成功保存，刷新后最终能显示新内容；短暂闪回默认首页不是数据库覆盖、Caddy 缓存或保存失败。
- `PublicHomeView` 在组件创建时直接用 `defaultHomepageContent`、默认项目、成员、奖项和赞助商初始化响应式状态，Vue 首屏立即渲染这些内置数据；`onMounted` 后才调用公开接口并覆盖状态，因此必然出现默认内容闪现。
- `fetchPublicHome` 同时请求首页、成员、项目、比赛和新闻五个接口，并在 `Promise.all` 全部完成后才统一更新页面；闪现时长取决于其中最慢的请求，生产首次访问或后端冷启动时会更明显。
- 推荐后续增加明确的首屏加载状态，并将核心首页配置和附属列表分阶段更新；本次按“为什么”的诊断请求未修改页面交互代码。

## 2026-08-26：游客邮箱/手机号注册与首页首屏内容同步

- 游客注册账号改为邮箱或手机号码二选一，前后端均进行格式校验；邮箱统一转换为小写，手机号码移除空格和短横线后保存，登录时执行相同归一化。
- 报名页在游客尚未提交报名表时，将注册账号自动填入“联系方式”字段，并保留手动修改能力；注册表单增加失焦校验、账号类型提示及无障碍错误关联。
- 新增 Flyway `V3__visitor_account_identifiers.sql`，将生产 MySQL 的 `accounts.username` 从 64 位扩展为 190 位；未改动已发布的 V1/V2，升级不会清空或覆盖现有账号及业务数据。
- 将当前主页编辑器中已保存的首屏标题、关于我们原则、成员栏说明和成果栏说明同步到前后端硬编码默认值，避免接口返回前短暂显示旧文案。
- 首页公开数据改为分阶段加载：首页配置与赞助商接口返回后立即更新文字，成员、项目、比赛和新闻随后并行刷新，保留 30 秒动态刷新机制。
- README 补充游客账号格式、归一化和报名联系方式自动填充说明。

### 验证

- Java 21 后端全量测试 19 项通过，0 失败、0 错误、0 跳过；新增测试覆盖普通用户名拒绝、邮箱小写归一化、格式化手机号注册与登录。
- Vue 生产构建通过，`git diff --check` 通过。
- 当前未发现可连接的本地浏览器会话，因此未执行自动化可视化检查；表单模板与生产构建已完成静态验证。

### 上线待办

- 提交并推送后等待 GitHub Actions 的 API/Web 镜像均发布成功，再在服务器拉取新镜像并运行部署脚本；API 启动时 Flyway 会自动执行 V3，现有 MySQL 数据保持不变。

## 2026-08-26：直接创建学生管理员与成员头像上传

- 在成员管理页新增“学生管理员”创建入口，系统管理员可填写登录账号、初始密码、姓名、成员编号、学籍、联系方式、状态和能力标签；后端固定创建 `CORE_STUDENT` 角色，避免前端篡改角色。
- 新账号使用 BCrypt 编码初始密码并在同一事务中建立成员档案；普通成员不能调用创建接口，核心学生登录后可立即访问系统管理模块。
- 个人主页编辑页将原头像 URL 输入替换为本地图片选择、即时预览、上传替换和移除；管理员也可在成员管理页协助维护成员头像。
- 头像仅支持 JPG、PNG、WebP，限制 4 MB，并在前后端校验格式；后端同时检查文件签名，使用成员 UUID 命名并在替换格式时清理旧文件。
- 生产头像目录为 `/var/lib/yeslab/uploads/members/avatars`，映射到宿主机 `/srv/yeslab/data/uploads`，现有上传文件备份脚本会自动纳入头像，无需新增数据库迁移。
- UI/UX Pro Max 指导本次使用圆形预览、明确文件限制、独立上传反馈、44px 操作目标和移动端纵向操作布局。

### 验证

- Java 21 后端全量测试 21 项通过，0 失败、0 错误、0 跳过；新增覆盖普通成员越权拦截、学生管理员创建后管理权限、头像上传、公开读取和移除。
- Vue 生产构建通过；成员管理与个人主页编辑页面均保持路由拆分。
- `git diff --check` 通过。

### 待办

- 当前由创建人通过可信渠道发送初始密码，尚未实现首次登录强制修改密码；正式多人使用前建议补充修改密码、管理员重置密码和首次改密标记。

## 2026-08-26：生产服务器日常更新说明

- 本地提交并推送 `main` 后，必须等待 GitHub Actions 的测试、API 镜像和 Web 镜像三个任务全部成功，再登录服务器更新，避免再次拉到构建尚未完成的旧 `latest` 镜像。
- 服务器统一在 root shell 中执行 `/opt/yes-lab/deploy/scripts/deploy.sh`；私有 GHCR 的 `docker login` 凭据按 Linux 用户隔离，因此登录和部署不能分别由 `ubuntu`、`root` 执行。
- 日常部署脚本会先备份正在运行的 MySQL 和上传文件，再执行 Git 快进更新、镜像拉取、MySQL/API/Web 顺序更新与 API 健康检查；不会删除 `/srv/yeslab/data`，也不得执行 `docker compose down -v`。
- 正式发布建议将 `deploy/.env.production` 的 `YESLAB_IMAGE_TAG` 固定为 Actions 发布的完整提交 SHA；使用 `latest` 更方便，但不如提交 SHA 易于确认版本和回滚。
- 本次仅复核并说明现有发布流程，没有连接或修改生产服务器；部署脚本、生产配置和业务代码均未改动。

## 2026-08-26：成果证书、个人主页展示与成员搜索完善

- 修复竞赛证书 JPG 上传：后端改为按 PDF、PNG、JPEG、WebP 文件签名识别真实类型，兼容 `image/jpg`、`image/pjpeg` 和 `application/octet-stream` 等浏览器 MIME 差异，同时继续拒绝伪装扩展名和超限文件。
- 已审核通过的比赛公开详情新增证书查看入口；待审核、驳回和未完成比赛的证书仍返回 404。证书使用 `inline` 响应，可在浏览器新标签中直接查看图片或 PDF。
- 成员个人主页编辑器新增“主页展示内容”，可选择、隐藏、上移和下移本人参与/指导且开启公开展示的项目，以及本人关联且已认证的比赛奖项；未主动配置过的旧账号继续默认展示全部合规记录。
- 新增 Flyway `V4__member_profile_showcase.sql`，只为成员表增加配置标记并新增项目、比赛两个有序关联表；不修改或删除已有账号、成员、项目、成果和上传文件。
- 项目负责人、指导老师、项目成员、项目管理员、比赛成员、比赛指导老师、招新面试官和首页成员配置均支持按姓名或学号/内部编号搜索；内部选项 DTO 增加编号字段。
- 隐私复核修正了项目内部/公开成员摘要映射：登录后的项目接口可提供编号以支持搜索，公开项目接口中的负责人、指导老师和成员编号均为空，不泄露真实学号。
- 首页会把完整公开数据库响应保存为浏览器公开快照，刷新时先同步应用快照再读取最新数据库，解决先显示旧内置演示内容再跳变的问题；首次无快照时先显示明确的同步状态，不渲染旧演示内容。页面加载时立即更新，保持打开时继续按 30 秒周期刷新，因此活跃访问下更新频率高于每日。内置源码数据仅作为首次访问且 API 不可用时的兜底。
- UI/UX Pro Max 用于本次表单与选择器设计：成员搜索保留可见标签、空结果反馈和原生选择控件，排序提供明确按钮与键盘替代，文件错误贴近输入显示，小屏布局改为纵向并保留足够点击区域。

### 验证

- Java 21 后端全量测试 22 项通过，0 失败、0 错误、0 跳过；覆盖异常 JPG MIME、未审核证书隔离、审核后公开查看、个人主页项目/奖项选择排序，以及公开项目不泄露成员编号。
- Vue 生产构建通过，共转换 1644 个模块；个人主页编辑器和成员搜索组件保持按路由拆分。
- `git diff --check` 通过；README、权限说明、模块边界和生产部署文档已同步更新。

### 上线待办

- 本地提交并推送后，等待 GitHub Actions 的测试、API 镜像和 Web 镜像全部成功，再由服务器 root 账号执行 `/opt/yes-lab/deploy/scripts/deploy.sh`；API 启动时 Flyway 自动执行 V4，无需手工 SQL。
- 本次未连接生产服务器，也未改动生产 MySQL 或上传目录；上线前仍应使用现有部署脚本完成自动备份。

## 2026-08-26：证书主图与成员搜索下拉交互修正

- 根据实际上传反馈再次调整 JPG 识别：标准文件签名之外，在前 4KB 内扫描 JPEG 起始标记；若扫描仪或移动端证书仍不标准，则仅对 `.jpg/.jpeg` 且客户端类型为常见 JPEG MIME 或二进制流的证书启用受限兼容。普通比赛图片保持严格签名校验，非 JPG 扩展名不能使用该兼容路径。
- 公开比赛模型增加证书内容类型。审核通过后，JPG/PNG 证书直接作为成果详情的主视觉图片完整展示，PDF 直接嵌入页面；移除原先作为附加入口的证书链接卡片，比赛现场图片降为后续图集。
- 重写 `SearchableMemberSelect`：由“独立搜索框 + 原生 select”改成单一可搜索下拉框，输入姓名或学号后在输入框下方实时显示候选人物、头像和编号，点击即可回填；支持方向键、回车、Esc、清除按钮、combobox/listbox ARIA 状态和必选校验。
- UI/UX Pro Max 指导本次使用单一输入焦点、输入框就近候选列表、至少 48px 选项高度、清晰的选中状态和完整键盘替代，避免之前搜索与选择割裂的问题。

### 验证

- Java 21 后端全量测试 23 项通过，0 失败、0 错误、0 跳过；新增覆盖非标准扫描 JPG 兼容、非 JPG 扩展名拒绝，以及公开证书内容类型。
- Vue 生产构建通过，共转换 1644 个模块；自定义搜索下拉组件和证书主图页面均完成编译。
- `git diff --check` 通过。
- 本地前后端服务健康，但当前没有可连接的浏览器实例，因此未执行自动点击与截图验证；没有将构建通过误报为浏览器交互通过。

### 上线待办

- 本次没有数据库结构变化；提交推送并等待 API/Web 镜像发布成功后，按原日常部署脚本更新即可。

## 2026-08-26：比赛图片、搜索布局、倒计时与图集删除

- 修复比赛图片 JPG 仍报“文件内容与格式不匹配”：比赛图集现与证书一样，优先识别标准签名并扫描前 4KB 的 JPEG 起始标记；仍无法识别时，仅对 `.jpg/.jpeg` 且 MIME 为常见 JPEG/二进制流的文件启用受限兼容。
- 搜索框文字重叠的直接原因是“只有搜索图标时，同一个 SVG 同时命中首个与最后一个图标定位规则”，加上竞赛表单通用 input padding 覆盖了组件留白。现改为搜索/下拉独立类名，并在组件层稳定保留左右图标空间。
- 新增全站右下角最近比赛倒计时，显示比赛名、赛道、省赛/国赛阶段、日期和剩余天数。未登录用户取全部未结束比赛的最近场次；登录成员只取自己担任队长、关联队员或指导老师的场次，系统管理员也不例外；游客账号不显示内部比赛。
- 倒计时的场次选择和天数计算统一以 `Asia/Shanghai` 日期边界为准；新建或修改比赛后会立即重新读取，跨日时也会自动更新。
- 比赛编辑页新增现有图集预览和单张删除。删除前需要二次确认，后端按图片 ID 校验队长/管理员权限，同时删除数据库关联与服务器存储文件；已审核记录仍只允许系统管理员修改。
- UI/UX Pro Max 用于本次搜索输入留白、固定浮层安全区、等宽数字、删除危险态、48px 触控目标、键盘焦点与减少动态效果复核。

### 验证

- Java 21 后端全量 23 项测试通过，0 失败、0 错误、0 跳过；覆盖非标准比赛 JPG、公开/本人倒计时、与比赛无关的管理员不可见，以及单张图集删除后资源返回 404。
- Vue 生产构建通过，共转换 1645 个模块；`git diff --check` 通过。
- 已尝试连接本地浏览器做点击与截图检查，但当前会话没有可用浏览器实例，因此未将编译通过误报为视觉验收通过。

### 上线待办

- 本次没有数据库迁移；推送后需等待 API 与 Web 镜像都发布成功，再在服务器执行日常部署脚本。图集删除是永久操作，上线前应保留现有自动备份流程。

## 2026-08-27：生产图片格式修复、上传预览与公开导航

- 直接测量 `yeslab.tech` 生产比赛详情：详情 JSON 为 1.4KB、约 1.97 秒完成；证书与图集均声明为 `image/jpeg`，但文件头实际为 BMP，单文件 4,249,654 字节。证书首字节约 0.98 秒，随后吞吐约 14KB/s，120 秒仅接收约 1.7MB；纯白区域并非数据库未保存，而是 BMP 内容被错误标记为 JPEG且原图传输过慢。
- 收紧后端 JPG 兼容规则：`.jpg/.jpeg` 且常见 JPEG MIME 的 BMP 扫描件会经 Java ImageIO 解码、限制最大边长与像素数、以 0.88 质量转成真正 JPEG；纯文本伪装 JPG 不再接受。JPEG 前导字节继续安全剥离，PDF/PNG/WebP 保持原格式。
- 对已经上传的 BMP/JPG 错配文件加入首次读取自动修复：先在同目录生成规范 JPEG 临时文件，再原子替换原文件；数据库记录、比赛 ID、图片 ID 和 URL 不变，不要求用户删除重传，也不新增数据库迁移。
- 比赛提交页新增证书与最多 8 张比赛图片的本地预览、说明编辑、单张移除和取消选择；BMP 内容会用正确的本地预览 MIME 展示。保存按钮明确显示“上传并由后端校验中”，前端只有在响应确认包含证书和对应数量图片后才跳转，比赛列表随后明确提示后端已确认接收的文件数量。
- 公开比赛详情为证书和图集增加明确的加载中、加载失败与新窗口重试状态，图片加载完成前保留稳定尺寸，避免无提示的大块白屏和布局跳动。
- 公共证书、图集、成员头像和项目封面增加一年 `public, immutable` 缓存；证书和项目封面 URL 增加更新时间版本参数，替换后自动换址。Caddy 对 Vite 哈希资源设置一年缓存，对 Logo、OG 图、赞助商和 Logo 备选图设置七天缓存。
- 首页顶栏新增“竞赛 / 新闻”导航，复用现有顶栏高度偏移逻辑，准确定位到 `#updates` 新闻与比赛双栏成果区，桌面端和移动端菜单同步生效。
- UI/UX Pro Max 用于本次本地预览、稳定图片占位、加载/错误反馈、触控按钮、替代文本、键盘焦点、响应式布局与减少动态效果复核。

### 验证

- Java 21 后端全量 24 项测试通过，0 失败、0 错误、0 跳过；新增覆盖 BMP 伪装 JPG 转成真实 JPEG、存量文件首次读取原子修复、伪装文本拒绝、公开图片与项目封面缓存头。
- Vue 生产构建通过，共转换 1645 个模块；上传本地预览、详情图片状态和首页新增导航均完成编译。
- `git diff --check` 通过。当前电脑未安装 Caddy CLI，因此只完成 Caddyfile 静态审查，配置需由 GitHub Actions 镜像构建及服务器容器启动继续验证。
- 当前会话无可用浏览器实例，无法执行自动点击和截图；生产格式、体积、首字节、吞吐和响应头均已通过直接 HTTP 请求确认。

### 上线说明

- 本次没有数据库迁移，也不修改 MySQL 业务数据。部署新 API 后，当前比赛 `b13331a4-f88a-47a9-ac34-a69d41f24b4f` 的证书和图集会在各自第一次访问时自动从约 4.25MB BMP 转成较小的真实 JPEG；存储文件属于现有上传备份范围。

## 2026-09-06：注册、报名表、成果头像、暗色模式与赞助商上传

### 完成内容

- 使用已安装的 UI/UX Pro Max 与 Emil Design Engineering skill，延续现有学院风格，补充语义颜色、可见焦点、按钮反馈、响应式布局和减少动态效果适配；检查了仓库遗留的 Sites 配置，正式部署仍以 `yeslab.tech` 的现有 Vue / Spring Boot / MySQL 方案为准。
- 注册页“游客注册”改为“新用户注册”，移除圈出的注册说明、账号说明和底部身份说明；仅允许邮箱注册，密码前后端统一限制 6—18 位，并在密码框下显示提示。原有内部账号与手机号登录归一化逻辑保留。
- 公开比赛详情为已绑定成员显示头像，姓名和头像共用公开个人主页链接；队长和已关联指导老师也增加头像与主页入口。无头像或加载失败时保留姓名首字/姓名。
- 增加全站亮色/暗色切换，覆盖首页、公开成员与比赛详情、认证页面、成员和管理后台；默认亮色，通过浏览器本地偏好在刷新、路由切换和同源标签页间保持一致。首屏在应用挂载前读取主题，存储被禁用时仍能切换。品牌图片与证书维持原色。
- 报名表将联系方式拆分为邮箱、手机号码、微信号，增加自我介绍；专业、班级和年级分别提示“软件工程”“24软件工程1班”“24级”。兴趣方向更新为无人机、机器人、视觉、嵌入式、硬件、新媒体、算法、深度学习，旧报名已选方向仍保留并允许取消。
- 招新接口与管理详情同步展示新字段；新增 Flyway `V5__recruitment_contact_details.sql`，添加四列并回填可识别的旧邮箱/手机号，不删除原联系方式。邮箱必填；手机、微信和自我介绍可按实际情况补充。
- “主页编辑 → 赞助伙伴”新增本地 Logo 上传与预览、独立上传反馈，保留名称、简介、官网、类型、合作方向及排序，并增加可编辑合作说明。上传成功后需保存整份主页配置才更新公开首页。
- Logo 上传接口受 `CONTENT_MANAGE` 权限保护，复用现有 JPG/PNG/WebP 文件签名与 4MB 限制；文件使用随机 UUID 地址和不可变缓存。生产目录 `/var/lib/yeslab/uploads/sponsors/avatars` 包含在现有上传持久化和备份目录内。README 已同步使用及升级说明。

### 验证结果

- Java 21 后端全量 25 项测试通过，0 失败、0 错误、0 跳过。更新测试覆盖邮箱格式、6/18 位有效密码与 5/19 位拒绝、报名新字段保存及管理员读取；新增赞助商测试覆盖未登录/普通成员上传拒绝、伪造格式及超限拒绝、公开图片读取/缓存和主页合作说明同步。
- Vue 生产构建通过，共转换 1648 个模块；`git diff --check` 通过。
- 通过独立脚本验证主题切换、本地存储、跨标签页同步及存储不可用时的行为；所有 CSS 变量均有定义，暗色主要文字在卡片上的对比度为 6.88:1—13.22:1。
- 本地原 H2 数据库启动暴露历史结构问题：成员表缺少 `showcase_configured` 字段。未手动删除或重建旧数据库；改用独立内存数据库和 `target/preview` 上传目录完成运行验证，API 健康状态 `UP`，前端同源代理健康检查 HTTP 200。
- 未执行浏览器点击/截图视觉验收；未在真实 MySQL 中演练 V5，不将构建或 H2 测试当作这两项验收通过。

### 待办与发布说明

- 本次只修改本地代码，未提交推送、发布遗留 Sites 展示站或修改正式服务器。上线需同时发布 API/Web，等待镜像构建完成后通过原先先备份再更新的部署脚本执行；Flyway 自动应用 V5。
- 生产上线前演练 V5 存量数据回填，并人工检查主要页面的亮暗主题和手机布局。
- 本地持久化 H2 的历史 `showcase_configured` 缺列问题仍待单独处理；此次运行验证使用临时演示数据，未迁移旧本地业务数据。

## 2026-09-07：生产服务器迁移可行性复核

### 结论

- 可以将现有正式站迁移到另一台服务器，但不能在 MySQL 运行时直接复制 `/srv/yeslab/data/mysql` 后启动。推荐迁移 `mysqldump` 逻辑备份、完整上传目录、生产环境配置和同版本代码/镜像。
- 现有 `backup.sh` 已生成可迁移的 `yeslab.sql`、`uploads.tar.gz` 和 `SHA256SUMS`；账号、报名、成员、项目、比赛、主页配置在 SQL 中，证书、比赛图集、项目封面、成员头像及赞助商 Logo 在上传压缩包中。
- `deploy/.env.production` 需要安全复制并保持 `0600` 权限，以保留数据库密码、JWT 密钥、镜像地址和域名设置；不应进入 Git、聊天记录或公开存储。保持 JWT 密钥和签发者可减少切换后已有登录状态受影响。
- Caddy 的证书卷无需复制。新服务器开放 TCP 80/443 后，将域名 A/AAAA 记录切到新 IP，Caddy 会自动申请和续期证书；切换前应降低 DNS TTL，并移除不再可用的旧 AAAA 记录。
- 当前 GitHub Actions 未声明多架构构建，镜像随 `ubuntu-latest` 构建，迁移目标应优先选择 `x86_64/amd64` Ubuntu 24.04；若选 ARM 服务器，需要先把工作流改为发布 `linux/amd64,linux/arm64` 多架构镜像。

### 推荐迁移顺序

- 先在新服务器完成 Docker/Compose、仓库、生产配置和同一提交 SHA 镜像准备，保持旧服务器继续服务。
- 正式切换时暂停旧服务器的 Web/API 写入，运行最后一次备份，校验 `SHA256SUMS` 后通过 SCP 或支持断点续传的 rsync 复制备份和配置。
- 在新服务器的空 `yeslab` 数据库导入 `yeslab.sql`，解压 `uploads.tar.gz` 到数据根目录，并恢复 MySQL/上传目录所有者；然后启动 API、检查 Flyway/Hibernate、健康接口和关键业务记录，最后启动 Web。
- 切换 DNS 后验证 HTTPS、登录、成员页、比赛图片和文件上传。旧服务器至少保留数天且停止写入，用于快速回切；新服务器开始接收写入后，不能直接回切到旧数据库，否则会丢失切换后的数据。

### 本次检查

- 核对 `compose.yaml`、备份/部署/首次安装脚本、生产环境示例、Caddyfile、GitHub Actions 和正式部署手册；本次仅提供迁移判断与操作边界，没有连接服务器、复制数据或修改部署代码。
- `git diff --check` 通过。现有工作区仍包含上一项功能修改，本次只追加开发日志，没有提交或推送。

## 2026-09-07：镜像跨服务器运行说明

- 核对当前镜像与 Compose：Web 镜像包含 Vue 静态文件和 Caddy，API 镜像包含 Java 21 应用；MySQL 使用官方 `mysql:8.4`，因此同架构 Linux 新服务器可以直接拉取并运行三个容器。
- 镜像不包含生产业务数据和秘密配置。只迁移镜像会得到空数据库和空上传目录；完整迁移仍需带上 `yeslab.sql`、`uploads.tar.gz` 和 `deploy/.env.production`，由 `compose.yaml` 挂载到新服务器持久目录。
- 推荐复用 GHCR 中以完整提交 SHA 标记的 Web/API 镜像，而不是在两台服务器之间复制 Docker 的内部文件；这样可以确认新旧服务器运行完全相同的程序版本。
- 当前 GitHub Actions 没有设置多架构输出，现有镜像按 GitHub `ubuntu-latest` 的 amd64 环境构建；新服务器应选 x86_64/amd64。ARM64 服务器需要先增加多架构构建。
- 本次为部署结构复核与说明，没有构建、推送或启动镜像；`git diff --check` 通过。

## 2026-09-07：云服务器整机镜像迁移说明

- 用户明确指的是云厂商的系统盘/整机镜像，不是 Docker 镜像。该方式可以迁移现有系统，但是否完整取决于 `/srv/yeslab/data` 是否位于被镜像包含的系统盘；若它属于独立数据盘，系统镜像不会自动包含 MySQL 和上传文件，必须同时创建数据盘快照或另行备份迁移。
- 制作镜像前仍应运行 `deploy/scripts/backup.sh`。为得到干净的一致性整机镜像，建议在短暂停机窗口依次停止 Web/API、正常停止 MySQL 容器，再关机后由云平台创建镜像；不建议在网站继续写入时制作系统镜像。
- 新服务器需与旧服务器保持 CPU 架构、引导方式和虚拟化驱动兼容，系统盘容量不得小于旧盘已用空间；跨云厂商还需确认其支持的 RAW/QCOW2/VMDK 镜像格式、cloud-init、网卡驱动和 DHCP 网络配置。
- 若整个根盘及 Docker 数据目录都包含在镜像中，Git 仓库、生产环境文件、Docker、容器镜像、Caddy 证书卷和 systemd 备份定时器通常会一起复制。新机启动后仍需显式执行 `docker compose ... up -d` 并检查容器、挂载、健康接口和证书续期，不能只以系统成功开机作为网站迁移完成。
- 同域名迁移时更新 A/AAAA 记录到新公网 IP；若改变域名，还需修改 `YESLAB_SITE_ADDRESS` 和 `YESLAB_CORS_ALLOWED_ORIGINS`，并让用户重新登录。新机对公网开放 80/443、保留 22，继续关闭 3306/8080。
- 本次仅复核整机镜像迁移边界，没有连接或操作生产服务器；具体可行性仍需根据新旧服务器的云厂商、CPU 架构和数据盘布局确认。

## 2026-09-07：服务器迁移方案选择

- 推荐采用“新服务器重新部署 + MySQL 逻辑备份恢复 + 上传目录迁移 + 生产配置迁移”，整机镜像仅作为辅助备份或同云厂商快速复制手段。
- 选择该方案是因为数据库和上传文件属于真正不可替代的业务数据；操作系统、Docker 和应用镜像均可重新安装或拉取。逻辑恢复也比跨机器直接复制 MySQL 数据目录更容易验证和回滚。
- 正式切换顺序应为：新机准备 → 旧机初始备份 → 停止旧站写入 → 最终备份与校验 → 复制 SQL/上传包/生产配置 → 新机恢复并验收 → 切换 DNS → 保留旧机只读回滚窗口。
- 若确认系统镜像包含 `/srv/yeslab/data` 且新旧主机兼容，整机镜像也可能直接启动成功，但仍必须先保存独立的 `yeslab.sql` 和 `uploads.tar.gz`，不能把整机镜像作为唯一数据库备份。

## 2026-09-07：新服务器 GHCR 拉取认证排障

- 新服务器首次运行引导脚本时，Docker 已正常安装，生产配置与数据目录也已创建；部署停止在拉取私有 `yes-lab-api` / `yes-lab-web` 镜像阶段，注册表返回 `unauthorized`。
- 核对 GitHub 最新 `main` 工作流运行 `34042784536` 已成功，排除镜像未发布或构建失败；Compose 中 MySQL/Web 的 `Interrupted` 是 API 拉取认证失败后并行拉取被取消的连锁状态。
- 根因是 GHCR 登录凭据按服务器和 Linux 用户分别保存在 Docker 配置中，新服务器不会继承旧服务器凭据。应在执行部署的同一个 `root` 用户下，用 GitHub 用户名 `KaoXiaoYu` 和含 `read:packages` 权限的 classic PAT 登录 `ghcr.io`，再重跑原引导命令。
- 已有 `/opt/yes-lab/deploy/.env.production` 会被幂等脚本保留；重新执行不会覆盖生产密钥，也不会删除 MySQL 或上传目录。本次仅完成只读诊断与操作说明，没有连接或修改新服务器。

## 2026-09-07：删除失败克隆后的重新拉取说明

- 按新服务器上的 `/opt/yes-lab` 已被删除处理：私有源码需使用仓库只读 Deploy Key 重新克隆，设置仓库级 `core.sshCommand` 后，后续部署脚本才能正常执行 `git fetch`。
- 后续终端截图已确认 GHCR 返回 `Login Succeeded`，API/Web 两个 `latest` 镜像均为 `Image is up to date`；当前唯一阻塞是 `/opt/yes-lab` 不存在，因而无法进入目录或运行其中的部署脚本。
- 新服务器进一步实测 SSH 克隆返回 `Permission denied (publickey)`；`/root/.ssh/yeslab_deploy` 与公钥文件存在且权限分别为 `0600/0644`，因此不是本地文件缺失或权限问题，而是该公钥尚未绑定到 `KaoXiaoYu/YES-Lab` 的 Deploy keys，或 GitHub 中绑定的是旧服务器公钥。
- Git 源码认证与 GHCR 镜像认证彼此独立。源码克隆成功后，仍需在运行部署的同一个 `root` 用户下用含 `read:packages` 的 classic PAT 登录 `ghcr.io`，再运行首次部署脚本。
- 如果删除整个仓库目录，目录内未提交到 Git 的 `deploy/.env.production` 也会一并丢失。全新且尚未启动数据库的服务器可以重新生成；迁移已有正式数据时必须从旧服务器安全复制原配置，以保持数据库密码、JWT 密钥和域名配置一致。
- `/srv/yeslab/data` 与 `/srv/yeslab/backups` 位于仓库外，删除 `/opt/yes-lab` 通常不会删除这两处数据；但重新部署前仍应先检查其内容，避免用新配置连接已有数据。本次未连接或更改服务器。

## 2026-09-07：私有仓库正式安装说明修订

- 重写正式部署手册的 Ubuntu 首次安装章节，默认采用当前“私有 Git 仓库 + 私有 GHCR”方案，不再先展示仅适合公开仓库的 HTTPS 克隆命令。
- 明确安装顺序：统一进入 root shell → 生成或复用新服务器 Deploy Key → 在 GitHub 仓库绑定公钥 → `ssh -T` 验证 → 克隆并固定仓库 SSH 命令 → 安装 Docker → 用 `read:packages` classic PAT 登录 GHCR → 重跑幂等引导脚本。
- 所有关键克隆命令改为可直接复制的单行形式，并增加成功文件检查；补充 Deploy Key 和 GHCR PAT 不能互相替代、Docker 登录凭据不跨服务器或 Linux 用户共享的说明。
- 新增 `No such file or directory`、`Permission denied (publickey)`、GHCR `unauthorized/denied`、Compose `Interrupted` 和克隆失败后 `git -C` 连锁报错的原因与处理表。
- README 与引导脚本失败提示同步更新，强调认证失败时可以安全重跑，不要删除 `/opt/yes-lab` 或未纳入 Git 的 `.env.production`。

### 验证

- `bootstrap-ubuntu.sh` 通过 `bash -n`；文档和脚本通过 `git diff --check`，并搜索确认已移除会误导私有仓库安装的旧 HTTPS 克隆及明文 PAT 示例。
- 本次只更新说明和错误提示，没有更改安装、数据库、备份或容器启动逻辑，也没有连接或修改服务器。

## 2026-09-07：已部署新服务器的数据迁移手册

- README 新增可直接执行的双服务器迁移步骤，明确区分旧服务器停写备份、SCP 传输、新服务器校验、上传目录恢复、数据库覆盖、服务启动和 DNS 切换。
- 正式部署手册增加完整迁移章节，说明 `yeslab.sql` 与 `uploads.tar.gz` 必须一起迁移，并覆盖无上传文件、非默认数据目录、UID/GID 权限、Flyway 增量迁移和切换后回滚边界。
- 新服务器在覆盖前先运行现有 `backup.sh` 保存当前数据库与上传目录；恢复数据库时继续使用新服务器已经初始化的 MySQL 凭据，避免整份旧环境文件覆盖后产生密码不一致。
- 旧服务器在最终备份前停止 Web/API、保持 MySQL 运行，完成后继续停止写入，避免两台服务器形成各自增长的数据分支；JWT 密钥仅作为可选的单项安全迁移，以减少用户重新登录。

### 验证

- `git diff --check` 通过；README 与正式部署手册中的迁移 Bash 代码块通过 `bash -n` 静态语法检查。
- 本次未连接两台生产服务器，也未执行数据库删除、导入、DNS 修改或服务切换；实际迁移需要将文档示例时间戳和新服务器 IP 换成真实值。

## 2026-09-07：跨服务器备份校验路径修复

- 定位新服务器执行 `sha256sum -c SHA256SUMS` 失败的原因：旧版 `backup.sh` 把旧服务器备份目录的绝对路径写入校验清单，SCP 到 `/srv/yeslab/migration` 后仍会查找原 `/srv/yeslab/backups` 路径，不能据此判断备份文件已经丢失。
- 修复 `backup.sh`，以后在备份目录内以相对文件名生成 `SHA256SUMS`，并显式只包含 `yeslab.sql` 与可选的 `uploads.tar.gz`。
- README 和正式部署手册改用兼容命令 `sed -E 's#  .*/#  #' SHA256SUMS | sha256sum -c -`，旧版绝对路径清单与新版相对路径清单都可在迁移目录中校验。

### 验证

- `backup.sh` 通过 `bash -n`；使用临时目录分别模拟绝对路径清单和相对路径清单，两种格式经兼容命令校验均显示 `OK`。
- `git diff --check` 通过；本次未连接服务器或执行数据库恢复。

## 2026-09-07：腾讯云域名解析到外部服务器说明

- 确认腾讯云注册的域名可以继续使用腾讯云云解析 DNS，并将 A 记录直接填写其他厂商服务器的公网 IPv4；域名注册商与网站服务器厂商不需要相同。
- 正式部署手册新增 `yeslab.tech` 的解析表：根域名 `@` 使用 A 记录指向新公网 IPv4，`www` 可使用 CNAME 指向根域名，默认线路 TTL 示例为 600。
- 补充迁移期间旧 A 记录、遗留 AAAA 记录、MX/TXT 记录的处理边界，以及 `dig` 和 `curl --resolve` 验证命令。
- 备案按实际服务器接入商办理：中国大陆其他厂商服务器应向该厂商申请首次备案或接入备案；腾讯云注册域名无需因此转移注册商。

### 验证

- 对照腾讯云当前云解析 DNS 与备案官方文档复核控制台路径、外部公网 IP 解析能力和“谁接入谁备案”原则。
- 文档 Bash 代码块通过 `bash -n` 静态语法检查，`git diff --check` 通过；本次没有登录腾讯云控制台或修改真实 DNS 记录。

## 2026-09-07：数据库迁移后登录 401 排查

- 从公网验证 `https://yeslab.tech/actuator/health` 返回 HTTP 200 和 `UP`，确认新服务器的 DNS、HTTPS、Caddy 反向代理与 API 已可用。
- 使用虚构账号向公开登录接口发起诊断请求，服务端返回预期的 HTTP 401 JSON“账号或密码错误”，确认请求已到达 Spring Boot 的登录校验，而不是代理层或跨域失败。
- 结合恢复流程定位首要原因：导入旧数据库会覆盖新服务器首次安装创建的同名管理员账号和密码哈希，新服务器首次安装时显示的随机密码随后不再有效，应使用旧服务器账号的原密码。
- 正式部署手册新增只读账号诊断命令，检查账号名、角色、启用状态和 BCrypt 哈希长度，不输出密码哈希；同时记录账号缺失、停用、哈希异常和 JWT 密钥变化的判断边界。

### 验证

- 线上健康接口和登录错误响应已实测；本地安全配置确认 `/api/v1/auth/login` 为匿名可访问，401 由认证服务在账号或密码不匹配时返回。
- 文档 Bash 代码块通过 `bash -n` 静态语法检查，`git diff --check` 通过；没有使用或收集真实账号密码，也没有修改生产数据库。

## 2026-09-07：迁移账号存在但旧密码仍返回 401

- 用户在新服务器只读查询确认迁移库包含 6 个旧账号，角色覆盖教师、核心学生和游客；所有账号均为启用状态，密码哈希长度均为标准 BCrypt 的 60 字符，因此可排除账号缺失、停用和字段截断。
- 新增不记录或显示密码的容器内登录测试：Shell 隐藏读取账号密码，由 Python 仅在管道内构造 JSON，再直接请求 API 容器，只输出 HTTP 状态码，用于区分浏览器状态与后端密码不匹配。
- 新增旧、新服务器凭据表聚合指纹查询，只输出 SHA-256 比较值，不暴露单个密码哈希；指纹相同且仍返回 401 时，应考虑旧浏览器依靠已有刷新会话登录、实际输入密码并非当前数据库密码。
- 补充无可用管理员时的恢复路径：先备份，再以不同账号名和内部编号重跑幂等引导脚本创建恢复管理员；禁止直接向 MySQL 写入明文密码或手工构造哈希。

### 验证

- 核对认证实现：登录接口使用迁移后的 `accounts.password_hash` 通过 Spring Security BCrypt 校验，账号不存在、停用或密码不匹配均返回 401；JWT 密钥只影响令牌，不参与密码比对。
- 文档迁移章节 Bash 代码块通过 `bash -n`，`git diff --check` 通过；没有取得真实密码、执行恢复管理员创建或修改生产数据。

## 2026-09-07：确认迁移账号密码在 API 中不匹配

- 用户对迁移账号 `19979652913` 执行隐藏输入的 API 容器内登录测试，结果为 HTTP 401；结合账号存在、启用且 BCrypt 哈希长度 60，确认问题是输入密码与迁移库保存的哈希不匹配，而不是浏览器、DNS、Caddy、CORS、JWT 或接口路由。
- 将无可用旧密码时的恢复方法改为直接调用现有生产管理员初始化器：先备份，生成随机 24 字节密码和唯一成员编号，只重建 API 创建 `recovery-admin`，查询确认后再次重建 API 清除初始化环境。
- 该恢复方法不修改旧账号、不直接写密码 SQL、不运行 apt、不拉取或升级镜像；恢复管理员可用于立即进入后台并核对迁移数据。

### 验证

- 静态核对 Compose 环境变量映射与 `InitialAdminBootstrapConfig`：只在显式启用时创建不存在的账号，使用应用 `PasswordEncoder`，已有账号不会被修改或提权。
- 恢复命令代码块通过 `bash -n`，`git diff --check` 通过；本次没有在生产服务器实际创建恢复管理员。


## 2026-09-07：UIUX 全站视觉改造与动画

### 完成内容

- 按用户“可以大幅修改 UI、增加动画、保持原有模块”的要求，使用 UI/UX Pro Max 的 Swiss Modernism 2.0 设计建议，保留学院海军蓝、暖金色和现有三色 YES Lab Logo，增加独立的 `src/refinement.css` 视觉层。
- 公开首页改为深色双栏首屏，新增轻量 `ResearchVisual` 组件，用本地 SVG、Lucide 图标和轨道线呈现自主飞行、空地协同与具身智能；研究方向目录调整为首屏底部卡片，保留原配置与跳转。
- 重做成果概览、项目筛选与卡片、实验室特色、团队与榜单、赞助商、新闻与竞赛成果的间距、圆角、字号和层次；现有栏目、内容配置、项目弹窗及成员详情入口全部保留。
- 登录注册页改为独立品牌面板与表单，手机保留紧凑品牌区域；成员、管理与公开详情页面统一标题区、卡片、表单字号和焦点样式。亮暗模式继续复用现有语义颜色，Logo 与证书不改色。
- 增加限时轨道入场、图形错峰出现、栏目渐显、项目封面悬停缩放和箭头反馈。减少动态效果偏好下禁用新装饰动画；键盘进入栏目时立即显示内容。
- 首页渐显改为内容就绪后初始化、限定在首页元素内；不支持 IntersectionObserver 或减少动态效果时保持内容可见。组件在请求期间卸载后不会再创建刷新定时器。
- 路由、权限、API、数据库及原有模块没有改动；未实现测验、积分或其他额外业务。保留工作区已有的部署与文档修改。

### 验证结果

- `npm run build` 通过，共转换 1650 个模块；新图形无新增依赖、外部图片请求或持续 JavaScript 动画循环。首次构建发现当前 Lucide 不含 Drone 导出，已替换为本地 SVG 并重新构建通过。
- `git diff --check` 通过；静态检查确认所有 CSS 变量均有定义，首页原有锚点无删除，业务路由、服务与后端文件无本次差异。
- 新首屏正文、次要文字、按钮及图形标注的颜色对比度分别为 15.94:1、10.05:1、10.85:1、7.99:1；亮暗卡片正文代表色组分别为 7.58:1、8.72:1。
- 本机 Vite 成功启动，首页与登录页 HTTP 均返回 200；完成手机、平板、桌面断点的静态样式审查，未执行浏览器截图、点击或真实视口验收，HTTP 成功不等同于视觉验收。

### 待办与发布说明

- 本次完成本地前端代码，未提交推送、部署 Sites 或修改正式服务器；上线按现有发布流程更新 Web 镜像，本次自身不要求 API 更新或数据库迁移。
- 正式发布前检查真实业务数据下的手机布局、长姓名/标题、亮暗主题、项目筛选和弹窗、登录注册与管理表单；本次未启动后端做登录写入联调。


## 2026-09-07：首页清爽科研风再设计与顶栏分区

### 设计与完成内容

- 根据用户对上一版美观度的反馈，先明确视觉方向；用户选择“清爽科研风：浅色留白、深蓝标题、突出项目内容”。实现前写入 `design-system/yes-lab/pages/public-home.md`，记录顶栏构图、首屏比例、栏目节奏、字体、色彩与动效规则。
- 顶栏改为品牌、主题与栏目导航、账号三个独立区域，亮暗切换在栏目跳转左侧；登录、注册与已登录账号固定在最右侧，不再嵌套于导航菜单。
- 1200px 以下使用两行顶栏：品牌和账号位于第一行，主题切换位于第二行栏目菜单左侧；菜单支持高度限制与滚动，添加 `aria-controls`，锚点偏移同步使用实际顶栏高度。登录注册始终可见。
- 首屏改为跟随主题的浅色留白、深蓝无衬线标题和蓝色主操作，替换上一版深色轨道卡片为无人机与地面机器人协同的等轴测 SVG 示意图；图形使用主题语义色，不增加外部素材或依赖。
- 项目卡片减轻阴影、统一文字层级，成员和合作伙伴区使用浅色内容表面，保留关于区与联系区作为深蓝分区；新闻栏移除面向访客的“手动排序”等实现说明。
- 保留原有模块、主页配置、项目筛选、弹窗、锚点、账号与权限逻辑；页内跳转在减少动态效果偏好下改为即时滚动。未修改后端或部署配置。

### 验证

- Vue 生产构建通过；模板 AST 检查通过：顶栏分组与顺序正确，主题位于导航之前，登录注册在菜单之外，全部原首页锚点保留。
- 全站 CSS 变量引用均有定义；`git diff --check` 通过。已静态复核桌面、1200px 折叠与手机两行顶栏布局。
- 未进行浏览器截图或真实点击验收，构建和模板结构检查不等同于实际视觉验收；未发布线上。

### 待办

- 发布前使用真实账号与主页内容检查手机右上角账号入口、长姓名省略、亮暗主题、栏目菜单及锚点定位。

## 2026-09-07：本地项目启动验证

- 安装前端依赖并启动 Vite 开发服务，监听 `http://127.0.0.1:5173/`。
- 本机原有 Java 17 不满足项目 Java 21 基线，已安装 Microsoft OpenJDK 21，并使用 Maven Wrapper 启动 Spring Boot API，监听 `http://127.0.0.1:8080/`。
- 前端与后端均保持运行；首次启动已下载 Maven 与项目依赖，并创建本地 H2 数据文件。

### 验证

- 访问前端首页返回 HTTP 200。
- 访问后端 `/actuator/health` 返回 HTTP 200。
- 通过 Vite 同源代理访问 `/api/v1/public/home` 返回 HTTP 200 和首页 JSON 数据，确认前后端联通。

### 待办

- 本次仅运行与验证本地开发环境，没有修改业务代码或发布线上；关闭当前开发进程后需重新启动前后端服务。

## 2026-09-07：首页概览实时更新与页内跳转

### 完成内容

- 将首页四项成果概览从静态展示改为根据公开 API 返回的竞赛成果、研究方向、合作伙伴与项目状态实时计算；后台数据变化后，首页每 30 秒自动同步，窗口重新获得焦点或页面恢复可见时立即同步。
- 四张概览卡片改为完整可点击链接：奖项跳转至竞赛成果，研究方向和状态跳转至研究项目，合作伙伴跳转至赞助伙伴；保留平滑滚动、固定顶栏偏移和减少动态效果偏好。
- 增加悬停、键盘焦点和方向箭头反馈，桌面、双列与单列断点继续保持原有分隔布局。

### 验证

- Vue 生产构建通过，共转换 1650 个模块；Vite HMR 已接收组件和样式更新。
- 本地前端 API 代理返回 HTTP 200，当前公开数据解析为 3 项奖项、3 个方向、1 家合作伙伴及 3 种项目状态。
- 已生成变更补丁、原始文件、修改后文件、验证记录和可执行回滚脚本；在隔离副本执行回滚后，3 个文件的 SHA-256 均与原始文件一致。

## 2026-09-07：首页概览移动端错位修复

### 完成内容

- 修复 430px 以下概览条仍被后加载的 760px 双列规则覆盖的问题，在最终样式层补充手机单列断点。
- 移除 `body` 的 320px 最小宽度限制，避免窄视口扣除滚动条宽度后出现横向滚动和内容偏移。
- 手机端四张实时概览卡片现在纵向排列，分隔线、箭头与整卡点击区域保持完整；431px 起仍使用双列布局。

### 验证

- 322px 视口下文档宽度从 320px 收敛到可视区 307px，横向溢出归零，四张卡片均为 267px 单列。
- 430px 断点为单列，431px 为双列；两者均无横向溢出。
- 点击 AWARDS 卡片可滚动到竞赛成果区，固定顶栏偏移正常；Vue 生产构建通过，共转换 1650 个模块。
- 已保存原始与修改后 CSS、补丁、验证记录及回滚脚本；隔离副本回滚后的 SHA-256 校验通过。

## 2026-09-07：前端动效、导航对齐与项目图片完善

### 完成内容

- 暗色切换增加圆形揭幕式 View Transition 动画；不支持该 API 的浏览器使用颜色与图标平滑过渡，并遵循减少动态效果偏好。
- 首屏无人机与地面机器人继续使用原生 SVG 绘制，加入入场、悬浮、行走、扫描、信号脉冲和路径流动动效。
- 顶栏栏目区在桌面与 1200px 以下两行布局中统一靠右对齐，保留移动端折叠菜单与账号入口。
- 首屏加入 3 个低透明度 YES Lab 多彩标识与轨道背景动效，增强品牌识别，同时保持正文可读性。
- 登录后的项目团队卡片增加固定比例图片区域与 `PROJECT IMAGE` 标识，支持已上传鉴权图片和默认品牌图，并增加轻微悬停缩放。

### 验证

- `pnpm run build` 通过，共转换 1650 个模块，退出码 0。
- 本地浏览器 639px 宽度验证：导航为 `flex-end`、无横向溢出；首页存在 1 个研究 SVG、3 个品牌标识，无人机、机器人及标识动画均已应用。
- 实际点击主题按钮后根主题变为 `dark`，按钮文案更新为“切换到亮色模式”。
- 使用教师演示账号登录并进入项目团队页，验证 1 张项目图片完整加载，尺寸 1920×819、可见，卡片标识存在。
- 已保存原始文件、修改后文件、补丁、SHA-256 清单、验证记录和回滚脚本；在隔离副本执行回滚后，8 个文件哈希全部匹配原始版本。

## 2026-09-07：首页空地协同 SVG 精细建模

### 完成内容

- 重绘首屏机械狗，参考 Unitree Go2 的长条低重心机身、前置传感器头、顶部感知模块与四足比例；补充顶部电池舱、侧壳、状态灯、摄像头、8 个关节、四组分段腿和足端结构。
- 重绘无人机为工程级四旋翼构型，补充 X 型粗机臂、4 个电机座、独立桨盘、中央电池机舱、RTK 天线、状态灯、双起落架和下置云台相机。
- 维持纯 SVG、主题语义色、暗色模式适配与响应式布局；新增长阴影与分面层次，并把桨叶旋转和四足交错步态拆成独立动画。
- 更新图形无障碍说明与工程概念标注，原有空地协同路径、扫描区域和实时连接表达继续保留。

### 验证

- `pnpm run build` 通过，共转换 1650 个模块，退出码 0。
- 浏览器检查确认：1 个研究 SVG、4 组旋翼、4 个电机、1 个云台、4 条分段腿、8 个关节和 1 组前置相机；旋翼与步态动画已应用。
- 暗色模式下完成实际截图检查，模型轮廓、传感器、关节与标注均清晰，无横向溢出。
- 已保存修改前文件、修改后文件、补丁、验证记录与回滚脚本；隔离副本回滚后的 2 个文件 SHA-256 全部匹配。

## 2026-09-07：修正首屏模型视觉验收
- 根据实际截图推翻箱式机器人，重绘圆角银色长机身、集成黑色传感器面及前后遮挡腿部；这是 SVG 插画而非精确三维模型。
- 停止整个旋翼组的平面旋转，保留水平桨盘并使用透明度与虚线流动表达转速；减弱地面网格和标注。
- 实际复查暗色截图，构建通过（1650 模块）；两文件隔离回滚字节匹配。产物位于 .codex-run/model-redesign。

## 2026-09-07：首屏切换真实 3D 模型
- 用 Three.js/GLTFLoader 替换研究图 SVG；接入 MuJoCo Menagerie 中 Go2 的真实网格、关节坐标与 Skydio X2 带贴图模型，许可和固定来源保存在 public/models。
- 转换为本地 GLB，Go2 33 个网格约 6.99 MB，无人机 5 个网格约 0.48 MB；分离四个旋翼并绕垂直轴旋转，支持悬浮、暂停、拖动/键盘旋转和重置。
- 添加环境光、PBR 材质、阴影、加载失败状态、减少动态偏好和离屏暂停渲染；卸载释放 GPU 资源。
- 实际复查亮暗截图与暂停、重置、键盘旋转；当前浏览器无横向溢出。构建通过（1659 模块），Three.js 懒加载分块大小有警告。
- 原版是 SVG，新版是 WebGL canvas；9 项文件修改副本/补丁/记录/隔离回滚验证通过。原有依赖审计有 27 个 moderate 提示，本次未批量升级。保留本地前后端，未发布线上。


## 2026-09-07：修复无人机旋翼离轴乱窜
- 根因是转换阶段分离出的两个旋翼节点保留了偏移局部原点，运行时统一修改 rotation.y 导致它们绕错误轴心公转。
- 移除四个导入旋翼节点的运行时旋转；来源模型自带半透明运动模糊桨盘，继续使用无人机整体悬浮表达运行状态。
- 浏览器连续观察 3.5 秒，四个桨盘均固定在电机位置；3D 加载状态为 fixed-to-motors。生产构建通过（1659 模块）。
- 修改副本、补丁、验证记录与回滚脚本保存在 .codex-run/rotor-axis-fix；隔离回滚字节校验通过。


## 2026-09-07：报名表增加个人展示与技术认知
- 报名表新增个人展示区：作品说明、最多 6 张 JPG/PNG/WebP 作品图片（单张 5MB）、最多 8 个个人主页或自媒体链接；后台招新管理可查看全部展示内容。
- 保留 20 道技术认知题库；系统按报名账号稳定随机分配 5 道，要求任选 3 道作答，也支持全部完成，前端实时显示答题进度，后端同步校验题目归属、去重与最低答题数。
- 新增鉴权图片上传、读取与删除接口、文件签名校验、本地存储和 V6 数据库迁移；旧报名数据保持兼容。
- 前端生产构建通过（1660 模块），后端完整测试通过；运行态 API 验证 5 题分配稳定、2 题提交返回 400、3 题提交成功、自媒体链接与作品图片上传成功。
- 浏览器实际注册访客并进入报名页，确认个人展示、图片选择、自媒体链接、随机 5 题与“已回答 3 / 5”的实时进度正常；本地前后端服务继续运行，未发布线上。

## 2026-09-07：桌面项目本地运行
- 按用户要求切换到 `C:\Users\FUNSKII\Desktop\YES-Lab` 并启动项目。
- 后端使用 Java 21 运行 `backend\mvnw.cmd -q spring-boot:run`，Tomcat 监听 `http://127.0.0.1:8080`，运行态健康检查 `/actuator/health` 返回 `UP`。
- 前端使用 `pnpm exec vite --host 127.0.0.1` 启动 Vite，监听 `http://127.0.0.1:5173/`。
- 验证结果：前端首页 HTTP 200，代理接口 `/api/v1/public/home` HTTP 200；项目当前保持本地运行。

## 2026-09-07：修复服务器更新启动失败
- 根据服务器日志定位到 `RecruitmentPortfolioStorageService` 初始化作品图片目录失败；容器开启 `read_only: true`，默认 `./data/recruitment` 在生产环境解析到 `/app/data/recruitment`，不可写。
- 新增生产配置 `yeslab.storage.recruitment-directory=${YESLAB_RECRUITMENT_DIRECTORY:/var/lib/yeslab/uploads/recruitment}`，并在 `compose.yaml` 的 api 服务中注入 `YESLAB_RECRUITMENT_DIRECTORY=/var/lib/yeslab/uploads/recruitment`。
- 修复后作品图片目录落到已有可写卷 `${YESLAB_DATA_ROOT:-/srv/yeslab/data}/uploads:/var/lib/yeslab/uploads` 下，和 sponsors/achievements/projects/members 上传目录保持一致。
- 验证：后端 `mvnw.cmd -q test` 通过，前端 `pnpm run build` 通过；补丁、修改副本、验证记录和回滚脚本保存于 `.codex-run/server-storage-fix`。
